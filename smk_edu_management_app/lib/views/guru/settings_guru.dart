import 'package:flutter/material.dart';

class SettingsGuru extends StatelessWidget {
  const SettingsGuru({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Settings guru"),),
    );
  }
}