import 'package:flutter/material.dart';

class TaskGuru extends StatelessWidget {
  const TaskGuru({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Task Guru")));
  }
}
