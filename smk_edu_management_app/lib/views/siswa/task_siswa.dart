import 'package:flutter/material.dart';

class TaskSiswa extends StatelessWidget {
  const TaskSiswa({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Task siswa")));
  }
}
